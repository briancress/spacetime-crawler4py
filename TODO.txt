1. Give different error responses based on the error
example: If the error message is that the server isn't responding, wait a certain amount of time then retry?

2. Filter out the urls that don't point to a webpage
 - It is important to filter out urls that do not point to a webpage. For
   example, PDFs, PPTs, css, js, etc. The is_valid filters a large number of
   such extensions, but there may be more.
 - It is important to filter out urls that are not with ics.uci.edu domain.

3. It is important to maintain the politeness to the cache server (on a per
   domain basis).

4. Understand the code